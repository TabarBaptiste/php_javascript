{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}
    <h2>Création d'une nouvelle roue</h2>
    <div class="container">
        <div class="row">
            <div class="col-0 col-sm-0 col-md-1 col-lg-2">{# rien c'est une marge #}</div>
            <div class="col-12 col-sm-12 col-md-10 col-lg-8">
                {{ form(form) }}
            </div>
            <div class="col-0 col-sm-0 col-md-1 col-lg-2">{# rien c'est une marge #}</div>
        </div>
    </div>
{% endblock %}

{# 
	<style>
		h2 {
			font-size: 24px;
			color: #333;
			margin-bottom: 20px;
			font-family: Arial, sans-serif;
		}

		div,
		p {
			font-family: Arial, sans-serif;
			background-color: #f5f5f5;
			padding: 5px;
			border-radius: 5px;
			width: 31%;
		}
	</style> #}
